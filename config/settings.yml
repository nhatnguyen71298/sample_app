user:
  name:
    max_length: 50
  email:
    max_length: 100
    regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  password:
    min_length: 6
  allow_nil: true
paginate:
  per_page: 5
avatar:
  size_users: 50
  size_show: 80
password_reset:
  expired: 2
